options:
  openstack-origin:
    default: distro
    type: string
    description: |
      Repository from which to install.  May be one of the following:
      distro (default), ppa:somecustom/ppa, a deb url sources entry,
      or a supported Cloud Archive release pocket.

      Supported Cloud Archive sources include: cloud:precise-folsom,
      cloud:precise-folsom/updates, cloud:precise-folsom/staging,
      cloud:precise-folsom/proposed.

      Note that updating this setting to a source that is known to
      provide a later version of OpenStack will trigger a software
      upgrade.
  openstack-install-branch:
    default: stable/train
    type: string
    description: |
      The git branch from which to install Magnum. You can pick one of the
      stable branches, or any of the tags.
  magnum-git-repo:
    default: https://github.com/openstack/magnum
    type: string
    description: |
      The magnum repo to use.
  rabbit-user:
    default: magnum
    type: string
    description: Username used to access rabbitmq queue
  rabbit-vhost:
    default: openstack
    type: string
    description: Rabbitmq vhost
  database-user:
    default: magnum
    type: string
    description: Username for Magnum database access
  database:
    default: magnum
    type: string
    description: Database name for Magnum
  debug:
    default: False
    type: boolean
    description: Enable debug logging
  verbose:
    default: False
    type: boolean
    description: Enable verbose logging
  region:
    default: RegionOne
    type: string
    description: OpenStack Region
  trustee-domain:
    type: string
    default: magnum
    description: Domain used for COE
  trustee-admin:
    type: string
    default: magnum_domain_admin
    description: Domain admin for the trustee-domain
